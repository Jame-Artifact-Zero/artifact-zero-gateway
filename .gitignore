# SQLite databases - never commit, causes CI lock failures
*.db
*.db-wal
*.db-shm
# Local startup scripts with credentials - never commit
az_start.bat
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
# Environment
.env
.env.*
# OS
.DS_Store
Thumbs.db
taskdef_edit.json
taskdef_clean.json
taskdef_current.json
taskdef_new.json
taskdef_final.json
# Dump files - never commit
*_dump.txt

# Local diagnostic scratch files - never commit
chk.py

# Cookie/session artifacts - never commit
cookies.txt

# Local AWS/ECS task definition drafts - never commit
cstore-task-def*.json
taskdef*.json

# Local operational runners with credentials - never commit
run_f500.ps1

# Local one-time seed scripts with keys/endpoints - never commit
seed_operator_memory.py
